Images a document references live here. An img line's path is normalised to
/media/<basename>, so a file dropped in this directory is reachable by its own
name and nothing above it is reachable at all.
